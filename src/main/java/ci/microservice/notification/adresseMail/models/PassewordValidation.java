/*
package ci.microservice.notification.adresseMail.models;

import javax.validation.ConstraintValidator;
import javax.validation.ConstraintValidatorContext;

public class PassewordValidation implements ConstraintValidator<Passeword, AdresseMail> {
    @Override
    public void initialize(Passeword constraintAnnotation) {

    }

    @Override
    public boolean isValid(AdresseMail value, ConstraintValidatorContext context) {
        if(value.getPassword() == null || value.getPassword().contains() ) return false;
    }
}
*/
